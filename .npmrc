# Deterministic, reproducible installs for a monorepo that ships to production.
engine-strict=false
fund=false
audit=false
# Never let a lifecycle script reach out to a blocked host and hang CI.
fetch-timeout=120000
